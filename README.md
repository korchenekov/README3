# Алгоритмы и структуры данных: сортировка и поиск

В этом документе представлены основные алгоритмы сортировки и поиска с подробными объяснениями принципов работы, временной сложности и примеров реализации.

## 📊 Алгоритмы сортировки

### 1. Сортировка выбором (Selection Sort)

**Определение:** Алгоритм сортировки, который на каждом шаге находит минимальный элемент в неотсортированной части массива и помещает его в начало этой части.

**Принцип работы:**
1. Массив делится на отсортированную (слева) и неотсортированную (справа) части
2. На каждом шаге находится минимальный элемент в неотсортированной части
3. Найденный минимальный элемент меняется местами с первым элементом неотсортированной части
4. Граница отсортированной части сдвигается на один элемент вправо

**Временная сложность:**
- Худший случай: O(n²)
- Средний случай: O(n²)
- Лучший случай: O(n²)
- Пространственная сложность: O(1)

**Пример пошагового выполнения:**

**Преимущества:**
- Прост в реализации
- Не требует дополнительной памяти
- Устойчив к одинаковым элементам

**Недостатки:**
- Медленный для больших массивов
- Всегда выполняет O(n²) сравнений

### 2. Сортировка пузырьком (Bubble Sort)

**Определение:** Простой алгоритм сортировки, который многократно проходит по массиву, сравнивая и меняя местами соседние элементы, если они находятся в неправильном порядке.

**Принцип работы:**
1. Последовательно сравниваются соседние элементы массива
2. Если левый элемент больше правого, они меняются местами
3. После каждого прохода самый большой элемент "всплывает" в конец массива
4. Процесс повторяется для оставшейся неотсортированной части

**Временная сложность:**
- Худший случай: O(n²)
- Средний случай: O(n²)
- Лучший случай: O(n) - для уже отсортированного массива
- Пространственная сложность: O(1)

**Пример пошагового выполнения:**

**Преимущества:**
- Прост в понимании и реализации
- Устойчивая сортировка
- Не требует дополнительной памяти

**Недостатки:**
- Очень медленный для больших массивов
- Неэффективен на практике

### 3. Сортировка вставками (Insertion Sort)

**Определение:** Алгоритм, который строит отсортированную последовательность, по одному элементу за раз, вставляя каждый новый элемент в правильную позицию.

**Принцип работы:**
1. Массив мысленно делится на отсортированную и неотсортированную части
2. На каждом шаге берется первый элемент из неотсортированной части
3. Элемент вставляется в отсортированную часть на правильную позицию
4. Элементы большие чем вставляемый сдвигаются вправо

**Временная сложность:**
- Худший случай: O(n²)
- Средний случай: O(n²)
- Лучший случай: O(n) - для уже отсортированного массива
- Пространственная сложность: O(1)


**Преимущества:**
- Эффективен для небольших массивов
- Устойчивая сортировка
- Адаптивный - быстрый на почти отсортированных массивах
- Онлайн-алгоритм - может сортировать по мере поступления данных

**Недостатки:**
- Медленный для больших массивов

### 4. Сортировка слиянием (Merge Sort)

**Определение:** Эффективный алгоритм сортировки, использующий стратегию "разделяй и властвуй".

**Принцип работы:**
1. Рекурсивно разделить массив на две половины до тех пор, пока каждый подмассив не будет содержать один элемент
2. Рекурсивно объединить отсортированные подмассивы для получения окончательного отсортированного массива

**Временная сложность:**
- Худший случай: O(n log n)
- Средний случай: O(n log n)
- Лучший случай: O(n log n)
- Пространственная сложность: O(n)


**Преимущества:**
- Гарантированная сложность O(n log n)
- Устойчивая сортировка
- Хорошо работает с большими массивами

**Недостатки:**
- Требует дополнительной памяти O(n)
- Не такой быстрый как быстрая сортировка на практике

### 5. Быстрая сортировка (Quick Sort)

**Определение:** Эффективный алгоритм сортировки, использующий стратегию "разделяй и властвуй" с выбором опорного элемента.

**Принцип работы:**
1. Выбрать опорный элемент из массива
2. Перераспределить элементы так, чтобы элементы меньше опорного оказались слева, а больше - справа
3. Рекурсивно применить алгоритм к двум подмассивам слева и справа от опорного элемента

**Временная сложность:**
- Худший случай: O(n²) - когда массив уже отсортирован и выбран плохой опорный
- Средний случай: O(n log n)
- Лучший случай: O(n log n)
- Пространственная сложность: O(log n)

**Преимущества:**
- Эффективнее чем сортировка вставками
- Сортировка на месте
- Хорошо работает на средних по размеру массивах

**Недостатки:**
- Сложность зависит от выбора последовательности шагов
- Не такой быстрый как более современные алгоритмы

## 🔍 Алгоритмы поиска

### 1. Линейный поиск (Linear Search)

**Определение:** Простейший алгоритм поиска, который последовательно проверяет каждый элемент массива.

**Принцип работы:**
1. Начать с первого элемента массива
2. Сравнить текущий элемент с искомым значением
3. Если элемент найден, вернуть его индекс
4. Если нет, перейти к следующему элементу
5. Продолжать до конца массива или до нахождения элемента

**Временная сложность:**
- Худший случай: O(n)
- Средний случай: O(n)
- Лучший случай: O(1)
- Пространственная сложность: O(1)

**Преимущества:**
- Очень быстрый для больших массивов
- Эффективное использование времени

**Недостатки:**
- Требует отсортированный массив
- Более сложная реализация чем у линейного поиска

### 3. Интерполяционный поиск (Interpolation Search)

**Определение:** Улучшенная версия бинарного поиска для равномерно распределенных данных.

**Принцип работы:**
1. Вместо деления массива пополам, оценивает позицию искомого элемента на основе его значения
2. Использует формулу: pos = low + ((target - arr[low]) * (high - low)) / (arr[high] - arr[low])
3. Работает аналогично бинарному поиску, но с более "умным" выбором позиции

**Временная сложность:**
- Худший случай: O(n) - когда данные распределены неравномерно
- Средний случай: O(log log n) - для равномерно распределенных данных
- Лучший случай: O(1)
- Пространственная сложность: O(1)
- 
**Преимущества:**
- Очень быстрый для равномерно распределенных данных
- Лучше бинарного поиска для больших равномерных массивов

**Недостатки:**
- Требует равномерно распределенные данные
- Медленнее бинарного поиска для неравномерных данных

### 4. Поиск Фибоначчи (Fibonacci Search)

**Определение:** Алгоритм поиска, использующий числа Фибоначчи для определения точек разделения массива.

**Принцип работы:**
1. Использует числа Фибоначчи для определения размера блоков поиска
2. Сравнивает искомый элемент с элементом в позиции F(k-1)
3. В зависимости от результата поиск продолжается в левой или правой части
4. Процесс повторяется с использованием меньших чисел Фибоначчи

**Временная сложность:**
- Худший случай: O(log n)
- Средний случай: O(log n)
- Лучший случай: O(1)
- Пространственная сложность: O(1)

**Преимущества:**
- Эффективен для больших массивов
- Избегает операции деления (использует только сложение и вычитание)

**Недостатки:**
- Более сложная реализация
- На практике не всегда лучше бинарного поиска

## 📈 Сравнительные таблицы

### Сравнение алгоритмов сортировки

| Алгоритм | Лучший случай | Средний случай | Худший случай | Память | Стабильность | Примечания |
|----------|---------------|----------------|---------------|--------|--------------|------------|
| Selection Sort | O(n²) | O(n²) | O(n²) | O(1) | Нет | Простой, но медленный |
| Bubble Sort | O(n) | O(n²) | O(n²) | O(1) | Да | Учебный алгоритм |
| Insertion Sort | O(n) | O(n²) | O(n²) | O(1) | Да | Хорош для маленьких массивов |
| Merge Sort | O(n log n) | O(n log n) | O(n log n) | O(n) | Да | Надежный, требует памяти |
| Quick Sort | O(n log n) | O(n log n) | O(n²) | O(log n) | Нет | Быстрый на практике |
| Heap Sort | O(n log n) | O(n log n) | O(n log n) | O(1) | Нет | Гарантированная сложность |
| Shell Sort | O(n log n) | O(n log n) | O(n²) | O(1) | Нет | Улучшенные вставки |

### Сравнение алгоритмов поиска

| Алгоритм | Лучший случай | Средний случай | Худший случай | Память | Условия | Примечания |
|----------|---------------|----------------|---------------|--------|---------|------------|
| Linear Search | O(1) | O(n) | O(n) | O(1) | Любой массив | Универсальный, но медленный |
| Binary Search | O(1) | O(log n) | O(log n) | O(1) | Отсортированный | Эффективный стандарт |
| Interpolation Search | O(1) | O(log log n) | O(n) | O(1) | Равномерное распределение | Очень быстрый для равномерных данных |
| Fibonacci Search | O(1) | O(log n) | O(log n) | O(1) | Отсортированный | Альтернатива бинарному поиску |

## 💡 Рекомендации по выбору алгоритма

### Для сортировки:
- **Малые массивы (n < 50):** Сортировка вставками
- **Средние массивы:** Быстрая сортировка или сортировка Шелла
- **Большие массивы:** Быстрая сортировка или пирамидальная сортировка
- **Когда важна стабильность:** Сортировка слиянием
- **Когда память ограничена:** Пирамидальная сортировка

### Для поиска:
- **Неотсортированные данные:** Линейный поиск
- **Отсортированные данные:** Бинарный поиск
- **Равномерно распределенные данные:** Интерполяционный поиск
- **Большие отсортированные массивы:** Бинарный или Фибоначчи поиск

